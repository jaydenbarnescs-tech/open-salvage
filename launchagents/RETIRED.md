# ⛔ RETIRED — 2026-07-19

These are the **source copies** of the salvage / mechatron / openSalvage launchd
plists. The system was retired on 2026-07-19. The installed copies (in
`~/Library/LaunchAgents`) have been `bootout` + `disable`d and renamed to
`*.plist.retired`. These source copies are inert (not in the launchd load path)
and are kept only for reference.

Do NOT re-run `install.sh` / re-load these unless you intend to revive the agent.
Full details + revival steps: `~/clawd/RETIRED.md`.
